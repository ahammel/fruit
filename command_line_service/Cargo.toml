[package]
name = "fruit-command-line-service"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "fruit"
path = "src/main.rs"

[dependencies]
fruit-domain = { path = "../domain" }
fruit-in-memory-db = { path = "../in_memory_db" }
rand = "0.8"
